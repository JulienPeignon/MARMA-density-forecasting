repos:
-   repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.6
    hooks:
    # Both hooks read ruff.toml: select = E,W,F,I,D with the pep257 convention.
    -   id: ruff
        types_or: [python]
        args: [--fix]
    -   id: ruff-format
        types_or: [python]

-   repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
    -   id: trailing-whitespace
    -   id: end-of-file-fixer
    -   id: check-yaml
    -   id: check-toml
    -   id: check-added-large-files
        args: ["--maxkb=10000"]
